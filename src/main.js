//Manejo del DOM
/*import { champDat } from './data/lol/lol.js';

const datosChampions = Object.values([champDat.data]);
console.log(datosChampions);    

//Imprimiendo cards en la página principal
function cardsLol(datosChampions){
    document.getElementById("cards_champs").innerHTML = cardsLol(datosChampions);
    let allCards = [];
    console.log(allCards);
    for(const item of datosChampions){
       allCards.push(`
    <div class="gallery__info">
       <img src="${item.splash}" alt="" class="img_champ"/>
         <h2 id="nameChamp" class="name_champ">${item.name}</h2>
         <h4 id="tagChamp" class="tags_champ">${item.tags.join("")}</h4>
    </div>
        <div class="gallery__reverse">
            <h3 id="titleChamp" class="gallery__title">${item.title}</h3><br>
            <h4 id="attackChamp" class="gallery__reverse">${item.attack}</h4>
            <h4 id="defenseChamp" class="gallery__reverse">${item.defense}</h4>
            <h4 id="magicChamp" class="gallery__reverse">${item.magic}</h4>
            <h4 id="difficultyChamp" class="gallery__reverse">${item.difficulty}</h4>
        </div>`);
    }
    return allCards;
    
}*/



/*peticion(championArray)
showMenu()
filter()
search()
ordenarAz(championArray)
ordenarZa(championArray)*/


/*import { peticion } from "./script/data.js";
import { showMenu } from "./script/menu.js";
import { filter } from "./script/filtros.js";
import { search } from "./script/busqueda.js";
import { ordenarAz } from "./script/ordenar-az.js";
import { ordenarZa } from "./script/ordenar-za.js";*/